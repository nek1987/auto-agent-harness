# ============================================================================
# Auto-Agent-Harness: Native Mode Configuration
# ============================================================================
#
# USE THIS FOR: Local development with Claude Pro/Max subscription
# AUTHENTICATION: Claude Code OAuth (run `claude login` first)
#
# QUICK START:
#   1. cp .env.native.example .env
#   2. claude login   (opens browser for OAuth)
#   3. ./start.sh     (Linux/macOS)
#      start.bat      (Windows)
#
# ============================================================================

# ----------------------------------------------------------------------------
# Authentication (UI Login)
# ----------------------------------------------------------------------------

# Enable authentication for the web UI
# Set to "false" for local-only development without login screen
AUTH_ENABLED=true

# JWT secret key for session tokens
# For development: leave empty (auto-generated on each restart)
# For persistence: generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=

# Default admin password (only used when no users exist)
# Login with: admin / admin
DEFAULT_ADMIN_PASSWORD=admin

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------

# Bind to localhost only (recommended for native mode)
HOST=127.0.0.1

# Server port - access UI at http://localhost:8888
PORT=8888

# Require localhost connections only (security)
# Keep "true" for native mode
REQUIRE_LOCALHOST=true

# ----------------------------------------------------------------------------
# Paths
# ----------------------------------------------------------------------------

# Data directory for settings, users, sessions
# Default: ~/.auto-agent-harness
# DATA_DIR=/custom/data/path

# Restrict file operations to specific directory (security sandbox)
# Native mode: typically not needed, leave commented
# ALLOWED_ROOT_DIRECTORY=/path/to/projects

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------

# Allowed origins for CORS (comma-separated, or "*" for all)
# Default: localhost only (if not set)
# Examples:
#   CORS_ORIGINS=*
#   CORS_ORIGINS=http://192.168.1.100:8888,http://myserver.local:8888
# CORS_ORIGINS=

# ----------------------------------------------------------------------------
# HTTPS/SSL Configuration
# ----------------------------------------------------------------------------

# Enable HTTPS (requires SSL_CERTFILE and SSL_KEYFILE)
# For HTTP (default): HTTPS_ENABLED=false
# For HTTPS: HTTPS_ENABLED=true
HTTPS_ENABLED=false

# Path to SSL certificate (PEM format)
# For self-signed: ./certs/server.crt
# For Let's Encrypt: /etc/letsencrypt/live/yourdomain.com/fullchain.pem
# SSL_CERTFILE=./certs/server.crt

# Path to SSL private key (PEM format)
# For self-signed: ./certs/server.key
# For Let's Encrypt: /etc/letsencrypt/live/yourdomain.com/privkey.pem
# SSL_KEYFILE=./certs/server.key

# ----------------------------------------------------------------------------
# Cookie Security
# ----------------------------------------------------------------------------

# Require HTTPS for cookies
# Set to "true" when using HTTPS, "false" for HTTP
COOKIE_SECURE=false

# SameSite cookie policy: strict, lax, or none
COOKIE_SAMESITE=lax

# ----------------------------------------------------------------------------
# Optional Integrations
# ----------------------------------------------------------------------------

# N8N webhook URL for progress notifications
# Sends POST request when test pass count increases
# PROGRESS_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/...
