# ============================================================================
# Auto-Agent-Harness Environment Configuration
# ============================================================================

# Authentication
# --------------
# Enable/disable authentication (default: true for production)
AUTH_ENABLED=true

# JWT secret key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# If not set, a random key is generated on startup (sessions won't persist across restarts)
JWT_SECRET_KEY=

# Default admin password (only used when no users exist)
DEFAULT_ADMIN_PASSWORD=admin

# Path Security
# -------------
# Restrict all file operations to this directory (recommended for production)
# If not set, all paths are allowed
ALLOWED_ROOT_DIRECTORY=/workspace

# Data directory for settings, users, sessions (always allowed)
# Default: ~/.autocoder
DATA_DIR=/app/data

# Require localhost connections only (set to false for remote access)
REQUIRE_LOCALHOST=false

# Server Configuration
# --------------------
# Server host (use 0.0.0.0 for docker/remote access)
HOST=0.0.0.0

# Server port
PORT=8888

# Webhooks (optional)
# -------------------
# N8N webhook URL for progress notifications
# PROGRESS_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/...

# Development
# -----------
# Enable debug mode
# DEBUG=false
